{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "packageRules": [
    {
      "description": "dotnet-sdk-runner tags read <sdk>-node<major>-<run>; docker versioning reads everything after the first dash as a compatibility suffix, so every newer tag looks incompatible and no upgrade is ever offered",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "ghcr.io/raeffs/dotnet-sdk-runner"
      ],
      "versioning": "regex:^(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)-node(?<build>\\d+)-(?<revision>\\d+)$"
    }
  ]
}
